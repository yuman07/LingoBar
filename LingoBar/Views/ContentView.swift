import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("LingoBar")
                .font(.title2)
                .fontWeight(.medium)
        }
        .frame(width: 400, height: 500)
    }
}
